#include "../writer/writer.hpp"
#include "encoder.hpp"

char symbols1[] = {
    'a', 'h', '1', 'l', '2', 'r', 'B', 'E', 'L'
    }; 

namespace System{
    const char* h;
    const char* Encoder::encode(const char* source){
        const char* result;
        const char* val;
        char* ptr = (char*) source;

        
    }   
}